# Provelume 0.10.1 — Emendatio release qualification

This matrix is the closed release gate for issue #251 and parent RELEASE_TRAIN #198. Exact-head
candidate and post-merge evidence must pass without changing tests or their resource limits.
Immutable evidence is reused only with its original timestamp and verified semantic applicability.

| Gate | Result and boundary |
| --- | --- |
| S01 Folder Sources and Windows network paths | PASS; merged #211 evidence retained in #198 |
| S02 per-Source exclusions and preview | PASS; merged #213 evidence retained in #198 |
| S03 guided read-only Google journey | OBSERVED on the authorized real account; original observation timestamp retained as `2026-09-11T23:19:51Z`; #215/#244 evidence retained |
| S04 canonical source and generated SVG/PNG/ICO | PASS; deterministic source, resource, associated-pixel, EN/IT layout and lifecycle checks |
| Native Windows subjective appearance | DEFERRED by the release owner; never recorded as PASS |
| Ruff, full pytest and permanent Ubuntu/Windows workflows | required on the exact candidate head and verified merge commit |
| Deterministic wheel/sdist and Windows installer | required at `0.10.1`; two clean builds and exact embedded identity |
| Published `0.10.0` upgrade | required; immutable public installer and wheel hashes remain unchanged |
| Manifest, SBOM, checksums, provenance and offline verification | required from the permanent release pipeline |
| Public asset redownload | required after publication; tag `v0.10.1` must resolve to the verified release-owner merge |

## Preserved evidence and limitations

The restored S03 evidence archive `S03-ripresa-consensi-completi-finale.zip` has SHA-256
`8f10162c1d77a45ccf30135cd34ec458c9e30d50adb313f87418c599316d326b`, contains 169 files,
and was independently restored and verified with credentials and Instance data excluded. Its real
Google result remains `OBSERVED`; release preparation changes identity and documentation only and
does not repeat provider operations or consent.

S04 merged as `dfb193faa27d8d989db41ae4d421a6731029bf62` after a fresh unchanged local FULL
(`1731 passed, 17 skipped`), deterministic Linux/Windows package rebuilds, exact Windows installer
assembly and all permanent workflows. The material native Windows appearance observation for
setup, window, Start, taskbar, About, existing tray, scaling and applicable appearance modes was
not performed. Publication therefore makes no subjective visual-quality claim. The later result
must be recorded separately without rewriting this release evidence.

The executable, installer and uninstaller remain explicitly unsigned. Signing, notarization and
verified publisher identity remain later work. Emendatio adds no provider, write scope, remote AI
fallback, Cura feature or background network behavior.

## Release-bundle verification

The permanent release pipeline must build twice from the exact verified merge commit, compare the
wheel and sdist byte for byte, build the Windows artifact, and assemble the manifest, CycloneDX
SBOM, checksums, provenance and offline verification evidence. Publication may use only that
bundle. Exact candidate head, merge commit, workflow and job IDs, artifact digests and independent
public redownload results belong in owner PR `OWNER_PR_PLACEHOLDER`, issue #251 and parent #198.
